apiVersion: v1
kind: Service
metadata:
  name: ascvd-fhir-data-extraction
  labels:
    app: ascvd-fhir-data-extraction
spec:
  ports:
    - port: 5000
      name: ascvd-fhir-data-extraction
      protocol: TCP
  selector:
    app: ascvd-fhir-data-extraction
  type: NodePort
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ascvd-fhir-data-extraction-ingress
  annotations:
    kubernetes.io/ingress.class: public-iks-k8s-nginx
         # IBM: public-iks-k8s-nginx
         # Azure: addon-http-application-routing
         # AWS: nginx
spec:
  rules:
    - host: atc-ascvd-fhir-data-extraction.integration-k8s-cluster-dcc48c44d831198cb8496b1ec68d7d12-0000.us-south.containers.appdomain.cloud
      http:
        paths:
          - path: /
            backend:
              serviceName: ascvd-fhir-data-extraction
              servicePort: 5000
---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: ascvd-fhir-data-extraction
spec:
  selector:
    matchLabels:
      app: ascvd-fhir-data-extraction
  template:
    metadata:
      labels:
        app: ascvd-fhir-data-extraction
    spec:
      containers:
        - name: ascvd-fhir-data-extraction
          image: docker.io/atclark/ascvd_fhir_data_extraction:0.0.1