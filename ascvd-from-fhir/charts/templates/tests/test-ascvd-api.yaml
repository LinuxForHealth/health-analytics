apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ascvd-from-fhir.fullname" . }}-test"
  labels:
    {{- include "ascvd-from-fhir.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: test
      image: busybox
      command: ['wget']
      args: ['{{ include "ascvd-from-fhir.fullname" . }}:{{ .Values.service.port }}/api/v1/health']
  restartPolicy: Never
